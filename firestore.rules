
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /students/{studentId} {
      // Allow public read and write access for the teacher dashboard.
      // In a production app, you would want more granular security here.
      allow read, write: if true;
    }
  }
}
