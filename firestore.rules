
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /students/{studentId} {
      // Anyone can read student data for the teacher dashboard
      allow read: if true;
      // Only the authenticated student can write to their own document
      allow write: if request.auth != null && request.auth.uid == studentId;
    }
  }
}
